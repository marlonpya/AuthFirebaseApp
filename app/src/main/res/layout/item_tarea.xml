<?xml version="1.0" encoding="utf-8"?>
<!-- Item de la lista de tareas: checkbox de completada + fecha límite, más iconos de editar/eliminar. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <CheckBox
            android:id="@+id/checkCompletada"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="Leer capítulo 3" />

        <TextView
            android:id="@+id/textFechaLimite"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="32dp"
            android:textSize="12sp"
            tools:text="Fecha límite: 15/07/2026" />

    </LinearLayout>

    <ImageButton
        android:id="@+id/botonEditarTarea"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/tareas_accion_editar"
        android:src="@android:drawable/ic_menu_edit" />

    <ImageButton
        android:id="@+id/botonEliminarTarea"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/tareas_accion_eliminar"
        android:src="@android:drawable/ic_menu_delete" />

</LinearLayout>
